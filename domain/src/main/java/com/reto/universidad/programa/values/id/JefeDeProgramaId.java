package com.reto.universidad.programa.values.id;

import co.com.sofka.domain.generic.Identity;

public class JefeDeProgramaId extends Identity {

    public JefeDeProgramaId(String id) {
        super(id);
    }
}
